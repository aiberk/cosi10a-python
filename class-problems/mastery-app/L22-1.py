# modify the following code so it uses an accumulator loop, not a list comprehension

# def divisors(n):
#     ''' return list of all divisors of n '''
#     divs = [d for d in range(2,n) if n%d==0]   # replace this line with an accumulator loop
#     return divs

# n = int(input("enter a positive number: "))
# print('divisors of',n,'are',divisors(n))
